{
  "engines": {
    "node": ">=8"
  },
  "name": "@babylonlabs-io/bitcoind-client",
  "version": "2.2.12",
  "description": "Bitcoin JSON-RPC client in javascript",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "prettier": "prettier --single-quote --trailing-comma es5 --write \"./**/*.js\"",
    "bitcoind": "docker run --net=host -p 18332:18332 -e DISABLEWALLET=0 kylemanna/bitcoind -testnet -rpcallowip=::/0 -rpcuser=test -rpcpassword=test"
  },
  "keywords": [
    "bitcoin",
    "bitcoind",
    "json-rpc",
    "jsonrpc",
    "client"
  ],
  "license": "MIT",
  "peerDependencies": {
    "isomorphic-fetch": "^3.0.0"
  },
  "devDependencies": {
    "isomorphic-fetch": "^3.0.0",
    "jest": "^29.7.0",
    "prettier": "1.11.1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/babylonlabs-io/bitcoind-client.git"
  }
}
